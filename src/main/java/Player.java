public class Player extends GameObject{

    public Player(int x, int y,int oldX,int oldY, int sizeWidth, int sizeHeight, char shape) {
        super(x, y, oldX, oldY, sizeWidth, sizeHeight, shape);
    }
}
